# Flat, npm-shaped node_modules — avoids pnpm's default symlink layout, which
# breaks module resolution for some bundlers/build tools.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
